sudo: false

language: node_js

install:
  - npm install
  - npm install coveralls -g

script:
  - npm run lint
  - npm run build
  - npm test

after_success:
 - nyc npm test && nyc report --reporter=text-lcov | coveralls

node_js:
  - "8"

dist: trusty
